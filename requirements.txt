# Public repo Python dependencies (interface deps intentionally excluded).

# Core training / inference
numpy
scipy
torch
torchvision
pytorch-lightning
timm
transformers
scikit-learn
tqdm
tensorboard
opencv-python

# Benchmark / reporting
pandas

# Dataset generation / geometry utilities
noise
timeout-decorator
huggingface_hub

# NOTE:
# OpenCascade bindings are required by dataset_tools and benchmark rendering code,
# but are installed with conda (not pip) in this project:
#   conda install -c conda-forge pythonocc-core=7.9.0
